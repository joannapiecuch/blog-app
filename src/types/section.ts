export interface SectionInterface {
  description: string;
  id: string;
  header: string;
  subHeader: string;
  assets: string[];
}

export interface SectionAssetInterface {
  id: string;
  overlayText: string;
}
